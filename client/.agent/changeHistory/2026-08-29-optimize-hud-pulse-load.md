# HUD / JavaFX Pulse 负载优化

- 日期：2026-08-29
- 任务目标：在不降低165Hz刷新目标、1696条FOV射线、FOV精度或画面质量的前提下，减少玩家模式下的JavaFX场景图失效和Pulse负载。
- 仓库：`O:\java\games\CS2D-MultiplayerUDP_Client`
- 分支：`main`
- 修改前基线：`8a8a14b8a436022ed55786fd5bf796abdfb3a22e`
- 实现状态：尚未提交，保留在工作区，等待实际游戏性能复测。

## 文件变更

### `src/main/java/cs2d/client/GameClient.java`（+58 / -3）

- 为装备栏增加可见状态签名缓存。
- 只有队伍、当前槽位、护甲、头盔、拆弹器或投掷物种类/数量变化时，才清空并重建装备SVG节点。
- 血量、位置、角度等高频状态更新不再触发装备节点树重建。
- 删除一条立即被共享效果覆盖的临时 `DropShadow` 创建。
- 为全局HUD可见性增加状态缓存，避免每帧遍历全部HUD节点并反复执行显示/隐藏。
- 增加 `setVisibleIfChanged`，只在可见性确实改变时写入JavaFX属性。
- 增加 `[HUD-CACHE]` 两项审计指标：装备节点重建次数、全局可见性遍历次数。
- 未修改渲染刷新率、FOV射线数量、FOV角度、交点算法、轮廓精度和视觉参数。

### `src/test/java/cs2d/client/GameClientProtocolTest.java`（+29 / -0）

- 新增装备HUD签名测试。
- 验证血量等无关字段变化不会触发重建。
- 验证切换槽位及投掷物数量变化一定会触发重建。

## 验证

- `mvn test`：通过。
- 结果：18个测试，0失败，0错误，0跳过。
- `git diff --check`：通过；仅存在仓库既有的LF/CRLF转换提示，没有空白错误。
- 客户端主代码及测试代码均由Maven重新编译成功。

## 手动性能复测

使用原有启动参数进入玩家模式，分别测试跟随视角和全局视角。稳定阶段预期：

- `[HUD-CACHE] 装备节点重建` 接近0；只有购买、拾取、切换槽位或消耗投掷物时增加。
- `[HUD-CACHE] 全局可见性遍历` 接近0；只在进入/退出游戏界面等状态切换时增加。
- 对比优化前的平均FPS、1% Low、严重迟帧和 `[P]`。

## 限制与后续

- 自动化测试不能测量真实JavaFX/Prism/GPU呈现性能，需要实际运行客户端复测。
- 本轮没有修改迷雾的EVEN_ODD合成路径。先用本次HUD缓存数据隔离收益，再决定是否迁移为独立迷雾图层，避免未经实测改变渲染结构。
- 回退点为基线提交 `8a8a14b8a436022ed55786fd5bf796abdfb3a22e`。
