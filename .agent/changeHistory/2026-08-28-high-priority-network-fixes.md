# 高优先级网络与同步修复记录

- 日期：2026-08-28
- 仓库：`O:\java\games\CS2D-MultiplayerUDP_Client`
- 修改前 HEAD：`cfdcf5b`
- 分支：`main`
- 范围：UDP 握手/地图恢复、预测位置、分片缓存安全

## 修改原因

首次欢迎包或地图包丢失后连接无法恢复；小更新会反复用旧坐标覆盖预测目标；缺片 UUID 永久驻留，且分片数量、索引、大小和解压尺寸均不受限。

## 文件级变更

### `src/main/java/cs2d/client/GameClient.java`（新增 133 行，删除 21 行）

- `myPlayerId` 改为跨线程可见，收到欢迎包后发送 `welcome_ack`。
- 每 750 ms 请求静态数据，直到真正收到 `map_data`；断开和重连时取消旧任务。
- 重连时清空旧地图引用，确保连接到另一台服务器时重新获取地图。
- 只有当前包实际含有有限的 `x/y` 时才更新 `targetX/targetY`，小包继续按速度外推。
- 分片限制为最多 8192 片、单片 1200 字符、并发缓存 128 组。
- 校验分片 ID、索引、总数、元数据一致性和 CRC32。
- 不完整缓存 5 秒过期，并由独立定时清理保证安静网络下也会释放。
- 压缩数据限制为 8 MiB，解压后限制为 32 MiB，防止压缩炸弹。

### `.agent/changeHistory/2026-08-28-high-priority-network-fixes.md`（新增 46 行，删除 0 行）

- 记录本次客户端改动、验证结果和回退基线，不影响运行时行为。

## 行为变化

- 欢迎包丢失时，客户端仍会重复加入；地图或任意地图分片丢失时会重新请求地图。
- 高频 `small_update` 不再把移动目标拉回上一次完整快照的位置。
- 过期、越界、过大、元数据冲突、CRC 错误和超大解压结果都会被丢弃。

## 验证

- `mvn -s C:\Users\小麦\.m2\settings.xml test`：BUILD SUCCESS（项目当前没有 JUnit 测试源）。
- 服务端生成 119 个真实 GZIP/CRC 分片，客户端重组器乱序恢复原文成功。
- 使用错误 CRC 的相同分片被客户端拒绝；负索引被拒绝。
- `git diff --check` 无新增空白错误；既有行尾转换提示未改变业务内容。

## 回退

- 修改前可回退点：`cfdcf5b`
- 本次改动将作为独立提交保存；回退时优先使用 `git revert <本次提交>`。
