# 僵尸安全出生与复活禁区统一校验

- 日期：2026-09-05
- 仓库：`O:/java/games/CS2D-MultiplayerUDP`
- 分支：`main`
- 修改前基线：`d49c26ad31b306ae1b3703b38ce4a5c70dec5b32`
- 目标：消除复活禁区、出生池、最终回退和重叠推离绕过校验而导致的地图外出生。
- 状态：本轮实现尚未提交。

## 基线处理

- 修改前已将上一轮验证通过的战斗反馈分类提交为 `d49c26a`，本轮从该恢复点开始。

## 逐文件变更

- `src/main/java/cs2d/server/SpawnPointValidator.java`（新增 92 行）：统一检查完整角色圆形的地图边界、20 像素复活禁区网格和障碍物相交；提供只返回已验证坐标的确定性应急扫描。
- `src/main/java/cs2d/server/GameState.java`（+68/-171）：出生区域不再因存在禁区而被跳过；预计算池改用统一验证；普通与僵尸随机/回退点禁止裸坐标；人类距离不足时选择最远合法点；重叠推离只有在新位置仍合法时才提交；删除旧中心点禁区检查入口。
- `src/test/java/cs2d/server/SpawnPointValidatorTest.java`（新增 38 行）：覆盖角色半径越界、身体擦入禁区、身体与墙相交、合法点及应急点再验证。
- `.agent/changeHistory/2026-09-05-safe-zombie-spawns.md`：本记录。

## 行为与接口

- 复活禁区按完整角色占地判定，不再只看中心所在单元格。
- 地图边缘按角色半径收缩，角色身体不能伸出地图矩形。
- 僵尸预计算出生池从生成时即保证静态合法；没有人类时也不会从未过滤池取点。
- 删除 `(width / 2, height / 2)` 和 `(width / 2, 50)` 两个未经验证的最终回退。
- 所有随机失败后的应急点仍经过同一验证器；地图完全没有合法点时明确拒绝生成并抛出配置错误。
- 未修改地图 JSON 结构、依赖或网络协议。没有复活禁区数据的地图仍需在编辑器中补画禁区，代码无法凭空推断异形地图的逻辑外部区域。

## 验证

- `mvn -q '-Dtest=SpawnPointValidatorTest' test`：通过。
- `mvn -q test`：服务器完整测试通过。
- `git diff --check`：通过；仅报告仓库既有 LF/CRLF 转换提示。

## 部署与回退

- 替换服务器构建并重启后生效；尚未启动真实多人波次进行地图级目测。
- 建议实机验证 `d3_2.5x` 两个带禁区版本，以及一张没有禁区数据的地图。
- 回退时以 `d49c26ad31b306ae1b3703b38ce4a5c70dec5b32` 为参照，仅撤销本记录文件。
